[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests requiring external services (Chrome, DB, Redis, etc.)
    slow: slow-running tests
asyncio_mode = auto

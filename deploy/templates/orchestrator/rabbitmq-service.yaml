apiVersion: v1
kind: Service
metadata:
  name: armada-rabbitmq
spec:
  selector:
    app: armada-rabbitmq
  ports:
  - port: 5672
    targetPort: 5672
    name: amqp
  - port: 15672
    targetPort: 15672
    name: management
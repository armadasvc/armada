apiVersion: v1
kind: Service
metadata:
  name: armada-frontend
  labels:
    app: armada-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: armada-frontend
